overwrite: true
schema:
  - schema.graphql
  - aws.graphql
generates:
  src/graphql/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      skipTypename: true
      avoidOptionals: true
